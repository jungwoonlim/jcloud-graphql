type Movie {
  id: Int!
  name: String!
  age: Int!
  gender: String!
  score: Int!
}

type Query {
  Movies: [Movie]!
  movie(id: Int!): Movie
}

type Mutation {
  addMovie(score: Int!, name: String!): Movie!
  deleteMovie(id: Int!): Boolean!
}
